# UPick
Nothing to see here

Build
------------
Clone with:
```bash
$ git clone https://github.com/mmhobi7/UPick.git -b master
```
Build with:
```bash
$ mkdir -p build
$ cd build
$ cmake ..
$ cmake --build .
```